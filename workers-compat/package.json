{
  "name": "workers-compat",
  "private": true,
  "type": "module",
  "description": "Proves @apet97/clockify-addon-sdk runs on Cloudflare Workers: import, instantiate, verify signatures in workerd, and bundle with wrangler.",
  "scripts": {
    "test": "vitest run",
    "bundle": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@apet97/clockify-addon-sdk": "file:../addon-sdk"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.0"
  }
}
